# Copie para .env.local e ajuste conforme seu ambiente.

# Backend Spring Boot — endpoint base com prefixo /v1
VITE_API_URL=http://localhost:8080/v1

# Servidor Express local (server/index.cjs) que executa o script Python
# via SSE. Usado apenas pela página "Novo antibiograma".
VITE_SCRIPT_SERVER_URL=http://localhost:3333
